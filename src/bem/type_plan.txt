Domain	<- Dimension *

Quadrature	<- Domain
		<- Order

		static init(void)
		static get_xi(void)
		static get_w(void)

LSet 	<- Domain
		<- nNode *

		static L_i(xi) (i = 1 ... nNode, xi in Domain)
		static dL_i(xi) (i = 1 ... nNode, xi in Domain)

Elem 	<- LSet
		<- Dim *

		coords

		x(xi)
		x'xi(xi)
		norm(xi)
		jac(xi)

Mapper 	<- Elem
		<- KernelInput

Integral <- Elem
		<- Kernel

		static eval<N>(Elem &e)
